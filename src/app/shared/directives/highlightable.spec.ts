import { Highlightable } from './highlightable';

describe('Highlightable', () => {
  it('should create an instance', () => {
    const directive = new Highlightable();
    expect(directive).toBeTruthy();
  });
});
